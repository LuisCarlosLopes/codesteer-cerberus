# Movido

Este arquivo virou dois:

- **`selector-health.md`** — a porta de qualidade (níveis A–D, bloqueio no D,
  escopo de linha). É o que esta skill acrescenta.
- **Mecânica de seletor** — delegada à skill oficial `playwright-cli`
  (`snapshot`, `generate-locator`, reference `test-generation.md`).

A versão anterior duplicava a documentação oficial do Playwright. Duplicata de
política é pior que ausência: quando as duas divergem, ninguém sabe qual vale.
